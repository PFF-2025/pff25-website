{% if faq.html and faq.title  %}
	<details class="faq">
		<summary>
			<h2 id="faq_{{ faq.id }}">
				{{ faq.title }}
			</h2>
		</summary>
		<div class="ck-content">
			{{ faq.html|raw }}
		</div>
	</details>
{% endif %}
